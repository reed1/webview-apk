plugins {
    id("com.android.application")
    id("org.jetbrains.kotlin.android")
}

android {
    namespace = "app.webviewapk"
    compileSdk = 35

    defaultConfig {
        applicationId = "{{ id }}"
        minSdk = 26
        targetSdk = 35
        versionCode = 1
        versionName = "1.0"

        buildConfigField("String", "APP_URL", "\"{{ url }}\"")
        buildConfigField("String", "APP_HOST", "\"{{ host }}\"")
        buildConfigField("long", "REFRESH_TIMEOUT_SEC", "{{ refresh_timeout_sec }}")
    }

    buildFeatures {
        buildConfig = true
    }

    buildTypes {
        release {
            isMinifyEnabled = false
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = "17"
    }
}

dependencies {
    implementation("androidx.core:core-ktx:1.15.0")
    implementation("androidx.webkit:webkit:1.12.1")
}
